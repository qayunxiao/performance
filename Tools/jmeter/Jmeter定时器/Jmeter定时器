同步定时器（同步点）：
    当在规定的时间内到达一定的线程数量，这些线程会在同一个时间点一起释放 瞬间产生很大压力
    场景高并发，抢购，抢红包， 注意线程组设置虚拟用户数量
    同步定时器：设置模拟用户组的数量和超时时间已毫秒为单位
常数吞吐量定时器：需要按指定的吞吐量发送请求时候使用
    吞吐量=设置450 * 线程数2 / 60秒 =15kn/s
固定定时器的使用: 需要指定时间之后再次请求，比如错误密码3次之后，需要间隔10分钟再次请求
    固定定时器：设置毫秒，针对上层请求有作用
