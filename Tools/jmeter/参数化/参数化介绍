本质：使用参数替代脚本固定的测试数据
实现方式：
    定义变量（最基础）
    文件定义方式（所有数据固定情况下）
    数据库的方式（灵活）
    函数的方式（灵活）

常见方式：
    用户定义的变量 - 位置：配置元件  - 用户定义的变量
    用户参数 -      位置：前置处理器  - 用户参数
    csv data set config 位置：配置元件 -csv data set config ，一个用户循环多次 每次参数不一样
    函数- 工具-函数助手对话框-选中 counter，设置FALSE，点击生成即可
